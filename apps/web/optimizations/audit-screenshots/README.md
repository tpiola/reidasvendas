# Capturas de auditoria

Geradas automaticamente pelo Playwright.

```bash
pnpm -C apps/web e2e site-audit-screenshots.spec.ts
```

Arquivos: rotas (`home-full-light.png`, `planos-dark.png`, …) e blocos da Home (`home-hero-light.png`, `home-faq-dark.png`, …).

Consulte `SITE_AUDIT.md` para a matriz de gaps P0/P1/P2.
